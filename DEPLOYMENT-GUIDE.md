# 🚀 دليل رفع موقع TM DTech على استضافة مجانية

## 📋 المحتويات

1. [اختيار منصة الاستضافة](#اختيار-منصة-الاستضافة)
2. [رفع الموقع على Vercel](#رفع-الموقع-على-vercel)
3. [رفع الموقع على Netlify](#رفع-الموقع-على-netlify)
4. [ربط الدومين الخاص](#ربط-الدومين-الخاص)
5. [بدائل أخرى للاستضافة](#بدائل-أخرى-للاستضافة)

---

## 🎯 اختيار منصة الاستضافة

### لماذا Netlify؟

| الميزة | Netlify | GitHub Pages | Vercel |
|--------|---------|--------------|--------|
| استضافة مجانية | ✅ | ✅ | ✅ |
| دومين خاص مجاني | ✅ | ✅ | ✅ |
| SSL مجاني | ✅ | ✅ | ✅ |
| سهولة الاستخدام | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |
| رفع مباشر (Drag & Drop) | ✅ | ❌ | ❌ |
| نماذج تواصل مجانية | ✅ | ❌ | ❌ |

**التوصية:** استخدم **Vercel** أو **Netlify** لأنهما الأسهل والأفضل للمواقع الثابتة.

---

## 📤 رفع الموقع على Vercel

### لماذا Vercel؟

- ✅ **سرعة فائقة** - شبكة CDN عالمية
- ✅ **نشر تلقائي** - من GitHub/GitLab/Bitbucket
- ✅ **SSL مجاني** - شهادة HTTPS تلقائية
- ✅ **دومين مجاني** - yoursite.vercel.app
- ✅ **تحليلات مجانية** - معرفة زوار موقعك
- ✅ **معاينة فورية** - لكل Pull Request

### الطريقة الأولى: عبر واجهة الويب (الأسهل)

#### الخطوة 1: إنشاء حساب على Vercel

1. اذهب إلى [vercel.com](https://vercel.com)
2. اضغط على **Sign Up**
3. اختر طريقة التسجيل:
   - **Continue with GitHub** (الموصى به)
   - Continue with GitLab
   - Continue with Bitbucket
   - Continue with Email

#### الخطوة 2: رفع الموقع من GitHub

1. بعد تسجيل الدخول، اضغط على **Add New...** > **Project**
2. اختر **Import Git Repository**
3. إذا لم يكن الموقع على GitHub بعد:
   - أنشئ Repository جديد على GitHub
   - ارفع ملفات المشروع (انظر الخطوات أدناه)
4. اختر Repository الموقع من القائمة
5. اضغط **Import**

#### الخطوة 3: إعدادات النشر

في صفحة Configure Project:

| الإعداد | القيمة |
|---------|--------|
| Project Name | `tm-dtech-website` |
| Framework Preset | `Other` |
| Root Directory | `.` (اتركه فارغاً) |
| Build Command | (اتركه فارغاً - موقع ثابت) |
| Output Directory | `.` (اتركه فارغاً) |

6. اضغط **Deploy**
7. انتظر 30-60 ثانية حتى يكتمل النشر

#### الخطوة 4: الحصول على الرابط

بعد النشر، ستحصل على رابط مثل:
```
https://tm-dtech-website.vercel.app
```

### الطريقة الثانية: عبر Vercel CLI (للمطورين)

#### الخطوة 1: تثبيت Vercel CLI

```bash
npm install -g vercel
```

#### الخطوة 2: تسجيل الدخول

```bash
vercel login
```

#### الخطوة 3: النشر

```bash
cd TM-DTech-Website
vercel
```

اتبع التعليمات:
- **Set up and deploy?** → `Y`
- **Which scope?** → اختر حسابك
- **Link to existing project?** → `N`
- **Project name?** → `tm-dtech-website`
- **Directory?** → `.`

#### الخطوة 4: النشر للإنتاج

```bash
vercel --prod
```

### إعدادات Vercel المتقدمة

الموقع يحتوي على ملف [`vercel.json`](vercel.json) جاهز يتضمن:

- ✅ إعدادات الأمان (Security Headers)
- ✅ التخزين المؤقت للملفات الثابتة
- ✅ إعادة كتابة الروابط النظيفة
- ✅ إعادة التوجيهات

### ربط الدومين الخاص في Vercel

1. اذهب إلى **Project Settings** > **Domains**
2. أدخل اسم الدومين (مثل: `tm-dtech.com`)
3. اضغط **Add**
4. ستحصل على سجلات DNS لإضافتها:

**للدومين الرئيسي:**
| النوع | الاسم | القيمة |
|-------|-------|--------|
| A | @ | 76.76.21.21 |

**للنطاق الفرعي www:**
| النوع | الاسم | القيمة |
|-------|-------|--------|
| CNAME | www | cname.vercel-dns.com |

5. أضف هذه السجلات في لوحة تحكم DNS الخاصة بدومينك
6. انتظر 1-24 ساعة للتفعيل
7. سيتم تفعيل SSL تلقائياً

---

## 📤 رفع الموقع على Netlify

### الطريقة الأولى: السحب والإفلات (الأسهل)

#### الخطوة 1: إنشاء حساب على Netlify

1. اذهب إلى [netlify.com](https://www.netlify.com)
2. اضغط على **Sign up**
3. يمكنك التسجيل باستخدام:
   - حساب GitHub
   - حساب GitLab
   - حساب Bitbucket
   - البريد الإلكتروني

#### الخطوة 2: رفع الموقع

1. بعد تسجيل الدخول، اذهب إلى **Sites**
2. سترى منطقة مكتوب عليها:
   ```
   "Drag and drop your site output folder here"
   ```
3. **اسحب مجلد `TM-DTech-Website` بالكامل** وأفلته في هذه المنطقة
4. انتظر حتى يكتمل الرفع (عادة أقل من دقيقة)

#### الخطوة 3: الحصول على الرابط

بعد الرفع، ستحصل على رابط مثل:
```
https://random-name-12345.netlify.app
```

#### الخطوة 4: تغيير اسم الموقع (اختياري)

1. اذهب إلى **Site settings**
2. اضغط على **Change site name**
3. اختر اسماً مميزاً مثل: `tm-dtech`
4. سيصبح الرابط: `https://tm-dtech.netlify.app`

---

### الطريقة الثانية: عبر GitHub (للتحديثات التلقائية)

#### الخطوة 1: رفع الموقع على GitHub

1. أنشئ حساب على [github.com](https://github.com) إذا لم يكن لديك
2. أنشئ Repository جديد باسم `tm-dtech-website`
3. ارفع ملفات الموقع

**باستخدام Git:**
```bash
cd TM-DTech-Website
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/USERNAME/tm-dtech-website.git
git push -u origin main
```

#### الخطوة 2: ربط GitHub مع Netlify

1. في Netlify، اضغط على **Add new site** > **Import an existing project**
2. اختر **GitHub**
3. اختر Repository الموقع
4. اضغط **Deploy site**

**الميزة:** أي تعديل على GitHub سيُنشر تلقائياً!

---

## 🌐 ربط الدومين الخاص

### المتطلبات

- دومين خاص (مثل: `tm-dtech.com`)
- الوصول إلى لوحة تحكم DNS الخاصة بالدومين

### الخطوة 1: إضافة الدومين في Netlify

1. اذهب إلى **Site settings** > **Domain management**
2. اضغط على **Add custom domain**
3. أدخل اسم الدومين الخاص بك (مثل: `tm-dtech.com`)
4. اضغط **Verify**
5. اضغط **Add domain**

### الخطوة 2: تعديل إعدادات DNS

#### الخيار A: استخدام Netlify DNS (الأسهل والموصى به)

1. في Netlify، اضغط على **Set up Netlify DNS**
2. ستحصل على 4 خوادم DNS مثل:
   ```
   dns1.p01.nsone.net
   dns2.p01.nsone.net
   dns3.p01.nsone.net
   dns4.p01.nsone.net
   ```
3. اذهب إلى موقع شراء الدومين (مثل: Namecheap, GoDaddy, Google Domains)
4. غيّر Nameservers إلى خوادم Netlify
5. انتظر 24-48 ساعة للتفعيل

#### الخيار B: استخدام سجلات DNS (إذا لم ترد تغيير Nameservers)

أضف السجلات التالية في لوحة تحكم DNS:

**للدومين الرئيسي (tm-dtech.com):**
| النوع | الاسم | القيمة |
|-------|-------|--------|
| A | @ | 75.2.60.5 |
| AAAA | @ | 2600:1f14:e22:5200::1 |

**للنطاق الفرعي www:**
| النوع | الاسم | القيمة |
|-------|-------|--------|
| CNAME | www | your-site-name.netlify.app |

### الخطوة 3: تفعيل HTTPS

1. بعد ربط الدومين، اذهب إلى **Domain management**
2. انزل إلى قسم **HTTPS**
3. اضغط على **Verify DNS configuration**
4. اضغط على **Provision certificate**
5. سيتم تفعيل شهادة SSL مجانية من Let's Encrypt

---

## 📝 إعدادات إضافية مهمة

### تفعيل نماذج التواصل (Netlify Forms)

لتفعيل نموذج التواصل في صفحة `contact.html`:

1. أضف `netlify` attribute للنموذج:

```html
<form name="contact" method="POST" data-netlify="true">
    <input type="hidden" name="form-name" value="contact">
    <!-- باقي حقول النموذج -->
</form>
```

2. ستصلك الرسائل في **Forms** في لوحة تحكم Netlify

### إعادة التوجيه (Redirects)

أنشئ ملف `_redirects` في المجلد الرئيسي:

```
# إعادة توجيه www إلى الدومين الرئيسي
https://www.tm-dtech.com/* https://tm-dtech.com/:splat 301!

# صفحة 404 مخصصة
/*    /index.html   404
```

### ملف netlify.toml (إعدادات متقدمة)

أنشئ ملف `netlify.toml` في المجلد الرئيسي:

```toml
[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"

[[redirects]]
  from = "https://www.tm-dtech.com/*"
  to = "https://tm-dtech.com/:splat"
  status = 301
  force = true
```

---

## 🔄 بدائل أخرى للاستضافة

### 1. GitHub Pages (مجاني)

**المميزات:**
- مجاني تماماً
- مرتبط بـ GitHub

**الخطوات:**
1. ارفع الموقع على GitHub
2. اذهب إلى **Settings** > **Pages**
3. اختر Branch: `main`
4. اضغط **Save**

**ربط الدومين:**
1. أنشئ ملف `CNAME` يحتوي على اسم الدومين
2. أضف سجل CNAME في DNS يشير إلى `username.github.io`

### 2. Vercel (مجاني) ⭐ موصى به

**انظر القسم المفصل أعلاه:** [رفع الموقع على Vercel](#رفع-الموقع-على-vercel)

**ملخص سريع:**
1. اذهب إلى [vercel.com](https://vercel.com)
2. سجل دخول بـ GitHub
3. اختر Repository
4. اضغط **Deploy**

### 3. Cloudflare Pages (مجاني)

**المميزات:**
- سرعة عالية جداً
- حماية DDoS مجانية

**الخطوات:**
1. اذهب إلى [pages.cloudflare.com](https://pages.cloudflare.com)
2. اربط حساب GitHub
3. اختر Repository
4. اضغط **Deploy**

### 4. Firebase Hosting (مجاني)

**الخطوات:**
```bash
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

---

## ❓ الأسئلة الشائعة

### كم يستغرق تفعيل الدومين؟
- تغيير Nameservers: 24-48 ساعة
- سجلات DNS: 1-24 ساعة

### هل الاستضافة مجانية فعلاً؟
نعم! Netlify يقدم:
- 100GB bandwidth شهرياً
- 300 دقيقة build شهرياً
- SSL مجاني
- دومين فرعي مجاني

### ماذا لو تجاوزت الحد المجاني؟
- Netlify سيُعلمك قبل الوصول للحد
- يمكنك الترقية للخطة المدفوعة ($19/شهر)
- أو استخدام استضافة أخرى

### كيف أحدث الموقع؟
- **طريقة السحب والإفلات:** ارفع المجلد مرة أخرى
- **طريقة GitHub:** ادفع التعديلات وسيُنشر تلقائياً

---

## 📞 الدعم

إذا واجهت أي مشكلة:
- [Netlify Docs](https://docs.netlify.com)
- [Netlify Community](https://answers.netlify.com)
- تواصل معنا: 01099710752

---

**تم إعداد هذا الدليل بواسطة TM DTech**

© 2026 TM DTech - Smart Digital Solutions
